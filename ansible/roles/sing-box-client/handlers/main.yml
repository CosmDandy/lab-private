---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart sing-box
  ansible.builtin.systemd:
    name: sing-box
    state: restarted

- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
